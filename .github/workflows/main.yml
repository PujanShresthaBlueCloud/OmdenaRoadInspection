name: Upgrade Packages
on:
  push:
    branches:
      - main

jobs:
  upgrade-packages:
    runs-on: ubuntu-latest

    steps:
    - name: Update packages
      run: |
        sudo apt-get update
        sudo apt-get upgrade -y
